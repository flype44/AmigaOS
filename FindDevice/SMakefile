# SAS/C 6.59 smakefile for FindDevice

COMPILER=sc
OUTFILE=FindDevice
OBJECTS=FindDevice.o

all: $(OUTFILE)

clean:
	@delete $(OUTFILE).o $(OUTFILE).lnk

aminet:
	@lha -a -r a $(OUTFILE).lha

$(OUTFILE): $(OBJECTS)
	$(COMPILER) LINK $(OBJECTS)
